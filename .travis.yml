# set environment
language: php

# php versions
php:
  - 7.1
  - 7.2
  - 7.3
  - hhvm

# before runs environment
before_script:
  - composer self-update
  - composer install --prefer-source --no-interaction --dev
